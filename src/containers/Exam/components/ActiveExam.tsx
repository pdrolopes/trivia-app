import React, { ReactElement } from "react";
import { ExamOngoing } from "store/exams";

type Props = {
  exam: ExamOngoing;
};

export default function ActiveExam(props: Props) {
  return <div></div>;
}
